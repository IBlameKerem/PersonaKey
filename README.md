# PersonaKey

PersonaKey generates strong, random passwords and immediately hashes them.
The hashed passwords can then be compared against existing hashes, e.g. to verify matches or detect duplicates without exposing the original password.

📌 Important: Please make sure to read the included disclaimer before using the project.
